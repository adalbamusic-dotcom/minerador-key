# Supabase baseline outputs

This directory receives timestamped schema dumps and structural audit reports.
Generated `.sql` and `.txt` files are ignored by Git until they have been
reviewed for scope and accidental sensitive content.

See `docs/SUPABASE_BASELINE.md` before generating or versioning a baseline.
