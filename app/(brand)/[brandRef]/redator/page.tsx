import { WriterPage as Entry } from "@/modules/redator"; import { requireTenantModule } from "../layout";
export default async function Page({ params, searchParams }: { params: Promise<{ brandRef: string }>; searchParams: Promise<{ articleId?: string | string[]; documentId?: string | string[] }> }) { await requireTenantModule((await params).brandRef, "redator"); const query = await searchParams; const articleId = Array.isArray(query.articleId) ? query.articleId[0] : query.articleId; const documentId = Array.isArray(query.documentId) ? query.documentId[0] : query.documentId; return <Entry initialArticleId={articleId || null} initialDocumentId={documentId || null} />; }
